pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r index.html css js public/
    - if [ -d images ]; then cp -r images public/; fi
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
